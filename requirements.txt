# Dependencies for the Chronos-2 layer-wise forecasting probes.  Python 3.11.
# Versions are those used to produce the committed forecasting results (the Narval
# Compute Canada builds, with the +computecanada local suffix dropped so they also
# resolve from PyPI for off-cluster / CPU reproduction).
#
# Off-cluster:  python -m venv .venv && source .venv/bin/activate
#               pip install -r requirements.txt          # pyarrow comes in transitively via `datasets`
#
# On Narval:    the .venv is already built from Compute Canada wheels — do NOT reinstall from
#               this file.  Just:  module load gcc python/3.11 arrow/24.0.0   (arrow supplies
#               pyarrow, which is not a pip package on the cluster)  &&  source .venv/bin/activate.
#               See README > Environment for the full recipe.

# --- Chronos-2 foundation model ---
torch==2.12.1                 # frozen encoder (CUDA on Narval A100 / MPS / CPU)
chronos-forecasting==2.3.1    # amazon/chronos-2 pipeline (pulls transformers, accelerate, tokenizers)

# --- forecasting datasets (autogluon/chronos_datasets: m4_hourly, monash_electricity_hourly, solar_1h) ---
datasets==5.0.0               # pulls pyarrow off-cluster; on Narval pyarrow is the `arrow` module

# --- probes + numerics + figures ---
numpy==1.26.4                 # <2 for Compute Canada wheel-stack compatibility
scikit-learn==1.7.1           # linear probes: Ridge / LogisticRegression / StandardScaler
matplotlib==3.10.8            # result figures
