# ---- environments ----
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo

# ---- large / generated artifacts ----
# Extracted Chronos-2 hidden-state features (~13 GB). Regenerate with `make regen-cache`
# or on-demand by running the experiments (they extract any missing (dataset, split,
# corruption, pooling) entry). See README > "Feature cache".
features_cache/

# ---- personal working notes (not for commits) ----
notes/EGOR_RESEARCH_NOTES.md

# ---- OS / editor cruft ----
.DS_Store
.ipynb_checkpoints/

# ---- secrets (none currently committed; keep them out) ----
.env
CLAUDE.md

# ---- SLURM run logs (ephemeral; provenance lives in the committed summaries/tables) ----
logs/
results/ext_v4_future_tokens/fslot_mlp/ptid_checkpoints/
# ext_v5 native-head adapters: 91 x ~2.4MB = ~215MB of Linear(768,768) checkpoints; deterministic
# (identity init + full-batch), regenerable via `run_native_head_adapter --adapt`. Metrics/plots are committed.
results/ext_v5_native_head_adapter/adapters/
